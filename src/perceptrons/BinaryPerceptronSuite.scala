package perceptrons

// attributes + 1 equal how many weights we have

// need training set "optdigits.train"
// need test set "optdigits.test"
// need design set "optdigits.names"

class BinaryPerceptron (val numAttributes: Int){

}